/**
 * NexaUI Hooks — Phase 1
 * Re-exports theme hooks and reserves surface for future hooks.
 */

export { useTheme, useColors, useTokens, useColorSchemeValue } from '../theme/useTheme';

// Placeholder exports for Phase 2/3 (keeps import paths stable)
export type { NexaTheme, ColorScheme } from '../theme/types';
